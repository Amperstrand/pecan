diff --git a/crates/cdk-mintd/src/lib.rs b/crates/cdk-mintd/src/lib.rs
index 4d9627e7..a289c1d2 100644
--- a/crates/cdk-mintd/src/lib.rs
+++ b/crates/cdk-mintd/src/lib.rs
@@ -26,7 +26,7 @@ use cdk::nuts::nut00::KnownMethod;
     feature = "bdk",
     feature = "grpc-processor"
 ))]
-use cdk::nuts::nut17::SupportedMethods;
+use cdk::nuts::nut17::{SupportedMethods, WsCommand};
 use cdk::nuts::nut19::{CachedEndpoint, Method as NUT19Method, Path as NUT19Path};
 use cdk::nuts::{
     AuthRequired, ContactInfo, Method, MintVersion, PaymentMethod, ProtectedEndpoint, RoutePath,
@@ -708,7 +708,7 @@ async fn configure_lightning_backend(
                 #[cfg(feature = "prometheus")]
                 let processor = MetricsMintPayment::new(processor);
 
-                mint_builder = configure_backend_for_unit(
+                mint_builder = configure_backend_for_managed_unit(
                     settings,
                     mint_builder,
                     ln_entry.unit.clone(),
@@ -937,6 +937,63 @@ async fn configure_backend_for_unit(
     .await
 }
 
+/// Configure one unit against a managed gRPC processor.
+///
+/// A managed processor can serve several configured units while the upstream
+/// settings RPC has a legacy singleton `unit` field. Requests still carry their
+/// unit and are rejected by the processor unless that unit is enabled. Skipping
+/// the singleton comparison here therefore removes only the stale handshake
+/// restriction, not request-time validation.
+async fn configure_backend_for_managed_unit(
+    settings: &config::Settings,
+    mut mint_builder: MintBuilder,
+    unit: cdk::nuts::CurrencyUnit,
+    mint_melt_limits: MintMeltLimits,
+    backend: Arc<dyn MintPayment<Err = cdk_common::payment::Error> + Send + Sync>,
+) -> Result<MintBuilder> {
+    let payment_settings = backend.get_settings().await?;
+    if let Some(unit_config) = settings
+        .grpc_processor
+        .as_ref()
+        .and_then(|grpc| grpc.unit_keysets.get(&unit.to_string()))
+    {
+        mint_builder.configure_unit(
+            unit.clone(),
+            cdk::mint::UnitConfig {
+                amounts: unit_config.amounts.clone(),
+                input_fee_ppk: unit_config.input_fee_ppk,
+            },
+        )?;
+        if let Some(final_expiry) = unit_config.initial_final_expiry {
+            mint_builder.configure_initial_keyset_expiry(unit.clone(), final_expiry);
+        }
+    }
+    let mut methods = Vec::new();
+
+    if payment_settings.bolt11.is_some() {
+        methods.push(PaymentMethod::Known(KnownMethod::Bolt11));
+    }
+    if payment_settings.bolt12.is_some() {
+        methods.push(PaymentMethod::Known(KnownMethod::Bolt12));
+    }
+    if payment_settings.onchain.is_some() {
+        methods.push(PaymentMethod::Known(KnownMethod::Onchain));
+    }
+    for method_name in payment_settings.custom.keys() {
+        methods.push(PaymentMethod::from(method_name.as_str()));
+    }
+
+    configure_backend_for_methods(
+        settings,
+        mint_builder,
+        unit,
+        mint_melt_limits,
+        backend,
+        methods,
+    )
+    .await
+}
+
 async fn configure_backend_for_methods(
     settings: &config::Settings,
     mut mint_builder: MintBuilder,
@@ -960,8 +1017,16 @@ async fn configure_backend_for_methods(
         let method_str = method.to_string();
         let nut17_supported = match method_str.as_str() {
             "bolt11" => SupportedMethods::default_bolt11(unit.clone()),
             "bolt12" => SupportedMethods::default_bolt12(unit.clone()),
-            _ => SupportedMethods::default_custom(method.clone(), unit.clone()),
+            _ if mint_melt_limits.mint_max == cdk::Amount::ZERO => SupportedMethods::new(
+                method.clone(),
+                unit.clone(),
+                vec![
+                    WsCommand::custom_melt_quote(&method_str),
+                    WsCommand::ProofState,
+                ],
+            ),
+            _ => SupportedMethods::default_custom(method.clone(), unit.clone()),
         };
         mint_builder = mint_builder.with_supported_websockets(nut17_supported);
     }
@@ -1719,7 +1784,17 @@ pub async fn run_mintd_with_shutdown(
                     if let Err(e) = tmp.init_from_db_if_present().await {
                         tracing::warn!("Failed to init builder from DB: {}", e);
                     }
-                    tmp
+                    // Payment capabilities are runtime configuration, not
+                    // append-only metadata. Clear stored entries before the
+                    // configured backends repopulate them, otherwise every
+                    // managed restart duplicates NUT-04/05/17 pairs.
+                    let mut info = tmp.current_mint_info();
+                    info.nuts.nut04.methods.clear();
+                    info.nuts.nut04.disabled = true;
+                    info.nuts.nut05.methods.clear();
+                    info.nuts.nut05.disabled = true;
+                    info.nuts.nut17.supported.clear();
+                    tmp.with_mint_info(info)
                 } else {
                     mint_builder
                 }
diff --git a/crates/cdk-mintd/src/config.rs b/crates/cdk-mintd/src/config.rs
index 9c0c2c3f..84c37821 100644
--- a/crates/cdk-mintd/src/config.rs
+++ b/crates/cdk-mintd/src/config.rs
@@ -1,4 +1,5 @@
+use std::collections::HashMap;
 use std::path::PathBuf;
 
 use bitcoin::hashes::{sha256, Hash};
 use cdk::nuts::{CurrencyUnit, PublicKey};
@@ -841,5 +842,14 @@ fn default_fake_wallet_supported_units() -> Vec<CurrencyUnit> {
     vec![CurrencyUnit::Sat]
 }
 
+#[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
+pub struct GrpcProcessorUnitKeyset {
+    pub amounts: Vec<u64>,
+    #[serde(default)]
+    pub input_fee_ppk: u64,
+    #[serde(default)]
+    pub initial_final_expiry: Option<u64>,
+}
+
 #[derive(Debug, Serialize, Deserialize, Clone, PartialEq)]
 pub struct GrpcProcessor {
@@ -846,5 +856,7 @@ pub struct GrpcProcessor {
     #[serde(default)]
     pub supported_units: Vec<CurrencyUnit>,
+    #[serde(default)]
+    pub unit_keysets: HashMap<String, GrpcProcessorUnitKeyset>,
     #[serde(default = "default_grpc_addr")]
     pub addr: String,
     #[serde(default = "default_grpc_port")]
@@ -858,5 +870,6 @@ impl Default for GrpcProcessor {
         Self {
             supported_units: Vec::new(),
+            unit_keysets: HashMap::new(),
             addr: default_grpc_addr(),
             port: default_grpc_port(),
             tls_dir: None,
diff --git a/crates/cdk-signatory/src/db_signatory.rs b/crates/cdk-signatory/src/db_signatory.rs
index 99cfe8a3..b484f5b6 100644
--- a/crates/cdk-signatory/src/db_signatory.rs
+++ b/crates/cdk-signatory/src/db_signatory.rs
@@ -81,6 +81,13 @@ impl DbSignatory {
         for mut info in self.localstore.get_keyset_infos().await? {
             let id = info.id;
             let keyset = self.generate_keyset(&info);
+            if keyset.id != id {
+                return Err(Error::Custom(format!(
+                    "stored keyset {id} cannot be derived from the configured recovery seed; \
+                     refusing to start with an inconsistent mint identity"
+                )));
+            }
+
             info.active = db_active_keysets.get(&info.unit) == Some(&info.id);
             if info.active {
                 active_keysets.insert(info.unit.clone(), id);
diff --git a/crates/cdk/src/mint/builder.rs b/crates/cdk/src/mint/builder.rs
index cd125790..c2d3f30a 100644
--- a/crates/cdk/src/mint/builder.rs
+++ b/crates/cdk/src/mint/builder.rs
@@ -69,6 +69,7 @@ pub struct MintBuilder {
     blind_auth_configured: bool,
     payment_processors: HashMap<PaymentProcessorKey, DynMintPayment>,
     supported_units: HashMap<CurrencyUnit, (u64, Vec<u64>)>,
+    initial_keyset_expiries: HashMap<CurrencyUnit, u64>,
     custom_paths: HashMap<CurrencyUnit, DerivationPath>,
     use_keyset_v2: Option<bool>,
     keyset_rotations: Vec<KeysetRotation>,
@@ -112,6 +113,7 @@ impl MintBuilder {
             blind_auth_configured: false,
             payment_processors: HashMap::new(),
             supported_units: HashMap::new(),
+            initial_keyset_expiries: HashMap::new(),
             custom_paths: HashMap::new(),
             use_keyset_v2: None,
             keyset_rotations: Vec::new(),
@@ -381,6 +383,11 @@ impl MintBuilder {
         Ok(())
     }
 
+    /// Set the final expiry used when the builder creates this unit's first keyset.
+    pub fn configure_initial_keyset_expiry(&mut self, unit: CurrencyUnit, final_expiry: u64) {
+        self.initial_keyset_expiries.insert(unit, final_expiry);
+    }
+
     /// Add a payment processor for the given unit and payment method
     ///
     /// If the unit has not been configured via [`configure_unit`](Self::configure_unit),
@@ -484,27 +491,31 @@ impl MintBuilder {
             PaymentMethod::Custom(_) => {
                 // Check if this custom method is supported by the payment processor
                 if settings.custom.contains_key(method.as_str()) {
-                    // Add to NUT04 (mint)
-                    let mint_method_settings = MintMethodSettings {
-                        method: method.clone(),
-                        unit: unit.clone(),
-                        min_amount: Some(limits.mint_min),
-                        max_amount: Some(limits.mint_max),
-                        options: Some(MintMethodOptions::Custom {}),
-                    };
-                    self.mint_info.nuts.nut04.methods.push(mint_method_settings);
-                    self.mint_info.nuts.nut04.disabled = false;
+                    if limits.mint_max != Amount::ZERO {
+                        // Add to NUT04 (mint)
+                        let mint_method_settings = MintMethodSettings {
+                            method: method.clone(),
+                            unit: unit.clone(),
+                            min_amount: Some(limits.mint_min),
+                            max_amount: Some(limits.mint_max),
+                            options: Some(MintMethodOptions::Custom {}),
+                        };
+                        self.mint_info.nuts.nut04.methods.push(mint_method_settings);
+                        self.mint_info.nuts.nut04.disabled = false;
+                    }
 
-                    // Add to NUT05 (melt)
-                    let melt_method_settings = MeltMethodSettings {
-                        method: method.clone(),
-                        unit: unit.clone(),
-                        min_amount: Some(limits.melt_min),
-                        max_amount: Some(limits.melt_max),
-                        options: None, // No custom-specific options in NUT05 yet
-                    };
-                    self.mint_info.nuts.nut05.methods.push(melt_method_settings);
-                    self.mint_info.nuts.nut05.disabled = false;
+                    if limits.melt_max != Amount::ZERO {
+                        // Add to NUT05 (melt)
+                        let melt_method_settings = MeltMethodSettings {
+                            method: method.clone(),
+                            unit: unit.clone(),
+                            min_amount: Some(limits.melt_min),
+                            max_amount: Some(limits.melt_max),
+                            options: None, // No custom-specific options in NUT05 yet
+                        };
+                        self.mint_info.nuts.nut05.methods.push(melt_method_settings);
+                        self.mint_info.nuts.nut05.disabled = false;
+                    }
                 }
             }
             // Handle onchain methods
@@ -593,6 +604,7 @@ impl MintBuilder {
                 .keysets
                 .iter()
                 .find(|k| k.active && k.unit == *unit);
+            let is_initial_keyset = keyset.is_none();
 
             let mut rotate = false;
 
@@ -647,7 +659,11 @@ impl MintBuilder {
                         } else {
                             cdk_common::nut02::KeySetVersion::Version00
                         },
-                        final_expiry: None,
+                        final_expiry: if is_initial_keyset {
+                            self.initial_keyset_expiries.get(unit).copied()
+                        } else {
+                            None
+                        },
                     })
                     .await?;
             }
diff --git a/crates/cdk/src/mint/mod.rs b/crates/cdk/src/mint/mod.rs
index 32b3a957..cdfd51e5 100644
--- a/crates/cdk/src/mint/mod.rs
+++ b/crates/cdk/src/mint/mod.rs
@@ -189,6 +189,23 @@ impl Mint {
                     }
                 }
 
+                // Runtime-managed fields must follow the current configuration.
+                // In particular, NUT-04/05/17 are the protocol source of truth
+                // for enabled method-unit capabilities and cannot remain pinned
+                // to the first process start.
+                stored.name = computed_info.name.clone();
+                stored.description = computed_info.description.clone();
+                stored.description_long = computed_info.description_long.clone();
+                stored.contact = computed_info.contact.clone();
+                stored.icon_url = computed_info.icon_url.clone();
+                stored.urls = computed_info.urls.clone();
+                stored.motd = computed_info.motd.clone();
+                stored.tos_url = computed_info.tos_url.clone();
+                stored.nuts.nut04 = computed_info.nuts.nut04.clone();
+                stored.nuts.nut05 = computed_info.nuts.nut05.clone();
+                stored.nuts.nut17 = computed_info.nuts.nut17.clone();
+                mutated = true;
+
                 if mutated {
                     let updated = serde_json::to_vec(&stored)?;
                     let mut tx = localstore.begin_transaction().await?;
diff --git a/crates/cdk/src/mint/issue/mod.rs b/crates/cdk/src/mint/issue/mod.rs
index 1707c28..fcefddb 100644
--- a/crates/cdk/src/mint/issue/mod.rs
+++ b/crates/cdk/src/mint/issue/mod.rs
@@ -302,12 +302,28 @@ impl Mint {
                     let mint_ttl = self.quote_ttl().await?.mint_ttl;
                     let quote_expiry = unix_time() + mint_ttl;
 
-                    // Convert extra serde_json::Value to JSON string if not null
-                    let extra_json = if request.extra.is_null() {
-                        None
-                    } else {
-                        Some(request.extra.to_string())
+                    // Forward the mint-generated quote id and the wallet's
+                    // NUT-20 pubkey to the payment processor as extra fields.
+                    // Backends that key their records on the quote id (e.g. a
+                    // managed teller processor matching quotes at a counter)
+                    // need both at creation time. Inserting after parsing the
+                    // wallet's extras means wallet-supplied fields of the same
+                    // names are overwritten and cannot spoof either value.
+                    let mut extra_map = match request.extra {
+                        serde_json::Value::Object(map) => map,
+                        _ => serde_json::Map::new(),
                     };
+                    extra_map.insert(
+                        "quote_id".to_string(),
+                        serde_json::Value::String(quote_id.to_string()),
+                    );
+                    if let Some(pk) = &pubkey {
+                        extra_map.insert(
+                            "pubkey".to_string(),
+                            serde_json::Value::String(pk.to_string()),
+                        );
+                    }
+                    let extra_json = Some(serde_json::Value::Object(extra_map).to_string());
 
                     let custom_options = CustomIncomingPaymentOptions {
                         method: payment_method.to_string(),
